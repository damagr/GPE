/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import GPENavigation from './components/GPENavigation';

AppRegistry.registerComponent(appName, () => GPENavigation);
